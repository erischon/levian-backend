import { authRoutes } from "./api/auth";
import { userRoutes } from "./api/user";
import {
  getProjectsRouter,
  getProjectByIdRouter,
  createProjectRouter,
  updateProjectRouter,
  deleteProjectRouter,
} from "./api/project";

export {
  authRoutes,
  userRoutes,
  getProjectsRouter,
  getProjectByIdRouter,
  createProjectRouter,
  updateProjectRouter,
  deleteProjectRouter,
};
