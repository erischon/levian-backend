import { authRoutes } from "./api/auth";
import { userRoutes } from "./api/user";
import {
  getProjectsRouter,
  getProjectByIdRouter,
  createProjectRouter,
} from "./api/project";

export {
  authRoutes,
  userRoutes,
  getProjectsRouter,
  getProjectByIdRouter,
  createProjectRouter,
};
